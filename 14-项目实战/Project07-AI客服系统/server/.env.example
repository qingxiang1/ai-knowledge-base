# OpenAI API Key（配置后由大模型基于「检索到的知识库事实」生成自然回复；留空则使用本地客服引擎，仍真实可用）
OPENAI_API_KEY=

# 模型名称（默认 gpt-4o-mini）
OPENAI_MODEL=gpt-4o-mini

# 自定义 API 地址（可选，兼容 OpenAI 协议的第三方服务，如 https://api.deepseek.com）
OPENAI_BASE_URL=

# 服务端口号（默认 3000）
PORT=3000
