# OpenAI API Key（配置后启用真实大模型的 ReAct 推理；留空则使用本地规则 Agent，仍会真实执行工具）
OPENAI_API_KEY=

# 模型名称（默认 gpt-4o-mini，可改为 gpt-4o 等）
OPENAI_MODEL=gpt-4o-mini

# 自定义 API 地址（可选，用于兼容 OpenAI 协议的第三方/代理服务，如 https://api.deepseek.com）
OPENAI_BASE_URL=

# 服务端口号（默认 8000）
PORT=8000
